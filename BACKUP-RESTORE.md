# Restore this backup

This private repository contains the saved working files as of 2026-09-08, including uncommitted work. Original Git history is not included.

Clone this repository, follow the project README and reinstall dependencies. Restore local environment variables and signing keys separately; excluded paths are listed in BACKUP-MANIFEST.json. Generated caches and build outputs were omitted. A sanitized workflow, if marked in the manifest, needs its credentials reconfigured.
